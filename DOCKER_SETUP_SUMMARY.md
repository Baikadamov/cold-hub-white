# 🐳 Docker Setup - Итоговая информация

## ✅ Что было сделано

Проект полностью настроен для деплоя через Docker. Теперь вы можете:

1. **Клонировать репозиторий на сервер**
2. **Запустить одной командой**: `docker compose up --build -d`
3. **Получить работающее приложение** на порту 3000

---

## 📁 Созданные файлы

### Основные файлы Docker

1. **`Dockerfile`** - Multi-stage сборка для оптимизации размера образа
   - Использует Node.js 20 Alpine (легкий образ)
   - Использует pnpm для управления зависимостями
   - Создает production-ready образ

2. **`docker-compose.yml`** - Конфигурация для запуска
   - Порт 3000
   - Автоматический перезапуск
   - Production окружение

3. **`.dockerignore`** - Исключения для Docker сборки
   - Игнорирует node_modules, .next и другие ненужные файлы

### Документация

4. **`README.md`** - Основная документация проекта
5. **`QUICK_START.md`** - Быстрый старт (самое важное)
6. **`DOCKER_README.md`** - Подробная инструкция по Docker
7. **`SERVER_SETUP.md`** - Настройка сервера с нуля
8. **`DEPLOYMENT_CHECKLIST.md`** - Чек-лист для деплоя

### Вспомогательные файлы

9. **`deploy.sh`** - Скрипт для автоматического деплоя
10. **`Makefile`** - Удобные команды (make deploy, make logs и т.д.)
11. **`nginx.conf.example`** - Пример конфигурации Nginx для HTTPS
12. **`.env.example`** - Пример переменных окружения

### Изменения в существующих файлах

13. **`next.config.mjs`** - Добавлен `output: 'standalone'` для Docker
14. **`.gitignore`** - Добавлено исключение для .env.example

---

## 🚀 Как использовать

### Самый простой способ (рекомендуется)

```bash
git clone <your-repo-url>
cd white_version
docker compose up --build -d
```

### С использованием скрипта

```bash
git clone <your-repo-url>
cd white_version
./deploy.sh
```

### С использованием Makefile

```bash
git clone <your-repo-url>
cd white_version
make deploy
```

---

## 🌐 Доступ к приложению

После запуска приложение будет доступно:

- **Локально**: http://localhost:3000
- **На сервере**: http://your-server-ip:3000

---

## 📊 Основные команды

```bash
# Запуск
docker compose up -d

# Остановка
docker compose down

# Логи
docker compose logs -f

# Статус
docker compose ps

# Перезапуск
docker compose restart

# Обновление
git pull && docker compose down && docker compose up --build -d
```

---

## 🔧 Технические детали

### Образ Docker
- **Base**: node:20-alpine
- **Package Manager**: pnpm
- **Build**: Multi-stage (deps → builder → runner)
- **Size**: Оптимизирован (~150-200 MB)
- **User**: Запускается от непривилегированного пользователя (nextjs)

### Порты
- **3000** - HTTP (приложение)

### Автозапуск
- Контейнер настроен на автоматический перезапуск (`restart: unless-stopped`)
- После перезагрузки сервера приложение запустится автоматически

---

## 📚 Документация

Начните с **`QUICK_START.md`** - там самое важное.

Для детальной настройки смотрите:
- `DOCKER_README.md` - все о Docker
- `SERVER_SETUP.md` - настройка сервера
- `DEPLOYMENT_CHECKLIST.md` - что проверить при деплое

---

## 🎯 Следующие шаги

1. **Протестируйте локально**:
   ```bash
   docker compose up --build
   ```

2. **Задеплойте на сервер**:
   - Установите Docker на сервер (см. SERVER_SETUP.md)
   - Клонируйте репозиторий
   - Запустите `docker compose up --build -d`

3. **Настройте HTTPS** (опционально):
   - Установите Nginx
   - Используйте `nginx.conf.example` как шаблон
   - Получите SSL сертификат через Let's Encrypt

---

## ✨ Готово!

Теперь ваше приложение готово к деплою. Просто:

```bash
git clone <repo> && cd white_version && docker compose up --build -d
```

И всё работает! 🎉

