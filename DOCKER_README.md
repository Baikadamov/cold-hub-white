# 🐳 Docker Deployment Guide

## Быстрый старт

### Требования
- Docker
- Docker Compose

### Запуск приложения

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd white_version
```

2. **Запустите приложение:**
```bash
docker compose up --build
```

3. **Откройте в браузере:**
```
http://localhost:3000
```

или если на сервере:
```
http://your-server-ip:3000
```

### Команды

**Запуск в фоновом режиме:**
```bash
docker compose up -d --build
```

**Остановка:**
```bash
docker compose down
```

**Просмотр логов:**
```bash
docker compose logs -f
```

**Перезапуск:**
```bash
docker compose restart
```

**Полная пересборка:**
```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

### Порты

Приложение доступно на порту **3000**.

Если нужно изменить порт, отредактируйте `docker-compose.yml`:
```yaml
ports:
  - "8080:3000"  # Теперь доступно на порту 8080
```

### Производственное развертывание

Для production рекомендуется:

1. **Использовать Nginx как reverse proxy** (для SSL/HTTPS)
2. **Настроить firewall**
3. **Использовать docker compose в detached режиме**

### Troubleshooting

**Проблема:** Порт 3000 уже занят
```bash
# Измените порт в docker-compose.yml или остановите процесс на порту 3000
lsof -ti:3000 | xargs kill -9
```

**Проблема:** Ошибки при сборке
```bash
# Очистите Docker кэш
docker system prune -a
docker compose build --no-cache
```

**Проблема:** Приложение не доступно извне
```bash
# Проверьте firewall
sudo ufw allow 3000
```

### Мониторинг

**Проверка статуса:**
```bash
docker compose ps
```

**Использование ресурсов:**
```bash
docker stats
```

